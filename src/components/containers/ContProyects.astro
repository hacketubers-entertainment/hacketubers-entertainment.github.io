---
// Esta es una referencia al componente ContProyectsUser
// Mantiene retrocompatibilidad con el código anterior
import ContProyectsUser from "./ContProyectsUser.astro";

interface Props {
  tecnologias?: boolean;
}

const { tecnologias } = Astro.props;
---

<ContProyectsUser mostrarTecnologias={tecnologias} />
